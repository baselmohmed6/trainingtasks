import { ChangecolorstyleDirective } from './changecolorstyle.directive';

describe('ChangecolorstyleDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangecolorstyleDirective();
    expect(directive).toBeTruthy();
  });
});
